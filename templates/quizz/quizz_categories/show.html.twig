{% extends 'base.html.twig' %}

{% block title %}QuizzCategories{% endblock %}

{% block body %}

        <h1>Categorie</h1>

    <div class="card">
        <div class="card-body">
            <h3 class="card-title">{{ quizz_category.id }}-{{ quizz_category.name }}</h3>
            <h5>Description :</h5>
            <p>{{ quizz_category.description }}</p>
        </div>
    </div>


    {% for quizz in quizz_category.quizzs %}
        <div class="card my-4">
            <div class="card-header">
                <h5>{{quizz.title}}</h5>
            </div>

            <div class="card-body">
                <p class="card-text">{{quizz.description}}</p>
                <a href="{{ path('app_quizz_quizz_show', {'id': quizz.id}) }}" class="btn btn-primary">Voir</a>
            </div>
        </div>
    {% endfor %}









    {# <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th> #}
                {# <td>{{ quizz_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quizz_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ quizz_category.description }}</td>
            </tr>
        </tbody>
    </table> #}

    {# {% for quizz in quizz_category.quizzs %}

    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h5 class="card-title">{{quizz.title}}</h5>
            <p class="card-text">{{quizz.description}}</p>
            <a href="{{ path('app_quizz_quizz_show', {'id': quizz.id}) }}" class="btn btn-primary">Voir</a>
        </div>
    </div>

    {% endfor %} #}


    <a href="{{ path('app_quizz_quizz_categories_index') }}" class="btn btn-primary">Retour</a>

    <a href="{{ path('app_quizz_quizz_categories_edit', {'id': quizz_category.id}) }}" class="btn btn-warning">edit</a>

{% endblock %}