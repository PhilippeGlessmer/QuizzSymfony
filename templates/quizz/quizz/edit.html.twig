{% extends 'base.html.twig' %}

{% block title %}Edit Quizz{% endblock %}

{% block body %}
    <h1>Edit Quizz</h1>

    {{ include('quizz/quizz/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_quizz_quizz_index') }}">back to list</a>

    {{ include('quizz/quizz/_delete_form.html.twig') }}

    {% for questions in quizz.quizzQuestions %}

    {{dump(questions)}}

        {# {% for reponse in  %}

            {{dump(reponse)}}

        {% endfor %} #}


    {% endfor %}

{% endblock %}
