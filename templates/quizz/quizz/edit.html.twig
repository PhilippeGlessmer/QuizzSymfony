{% extends 'base.html.twig' %}

{% block title %}Edit Quizz{% endblock %}

{% block body %}
    <h1>Edit Quizz</h1>

    <div class="card">
        <h5 class="card-header">
        Formulaire : 
        </h5>
        <div class="card-body">
            {{ include('quizz/quizz/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
        <div class="card-footer d-flex">
            <a href="{{ path('app_quizz_quizz_index') }}" class="btn btn-primary mx-2">Retour</a>

            {{ include('quizz/quizz/_delete_form.html.twig') }}
        </div>

    </div>



{% endblock %}
