{% extends 'base.html.twig' %}

{% block title %}QuizzChoices{% endblock %}

{% block body %}
    <h1>QuizzChoices</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quizz_choice.id }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ quizz_choice.reponse }}</td>
            </tr>
            <tr>
                <th>IsCorrect</th>
                <td>{{ quizz_choice.isCorrect ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quizz_quizz_choices_index') }}">back to list</a>

    <a href="{{ path('app_quizz_quizz_choices_edit', {'id': quizz_choice.id}) }}">edit</a>

    {{ include('quizz/quizz_choices/_delete_form.html.twig') }}
{% endblock %}
