<form method="post" action="{{ path('app_quizz_quizz_choices_delete', {'id': quizz_choice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quizz_choice.id) }}">
    <button class="btn">Delete</button>
</form>
