{% extends 'base.html.twig' %}

{% block title %}QuizzUserAnswers index{% endblock %}

{% block body %}
    <h1>QuizzUserAnswers index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for quizz_user_answer in quizz_user_answers %}
            <tr>
                <td>{{ quizz_user_answer.id }}</td>
                <td>
                    <a href="{{ path('app_quizz_quizz_user_answers_show', {'id': quizz_user_answer.id}) }}">show</a>
                    <a href="{{ path('app_quizz_quizz_user_answers_edit', {'id': quizz_user_answer.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_quizz_quizz_user_answers_new') }}">Create new</a>
{% endblock %}
