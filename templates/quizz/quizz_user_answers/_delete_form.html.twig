<form method="post" action="{{ path('app_quizz_quizz_user_answers_delete', {'id': quizz_user_answer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quizz_user_answer.id) }}">
    <button class="btn">Delete</button>
</form>
